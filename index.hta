<!DOCTYPE html>
<html>
<head>

</head>
<body>
  <script type="text/javascript" src="app/config/DatabaseConfig.js"></script>
  <script type="text/javascript" src="app/tpl/services/Database.js"></script>
  <script src="app/tpl/models/UserModel.js"></script>
  <pre id="output">Loading...</pre>
  <script type="text/javascript">
  function ObjectToString(obj) {
  var str = "";
  for (var key in obj) {
    if (obj.hasOwnProperty(key)) {
      str += key + ": " + obj[key] + "\n";
    }
  }
  return str;
}

  var db = new Database("database");

  // Get all users with rank_id 4
  var users = db.select("rank_id", 4, "users");

  // Get all users with username 'claydk'
  var users = db.select("username", "claydk", "users");

  // Get the user with id 1 (assumes id is unique, so you may want the first result)
  var user = db.select("id", 1, "__details")[0] || null;

  var users = db.selectAll("__details")[0];

  document.getElementById("output").innerHTML = users.name;
  </script>

</body>
